spring:
  application:
    name: my-app1
  cloud:
    kubernetes:
      config:
        name: app1-config
    reload:
      enabled: true
      strategy: restart_context
management:
  endpoint:
    restart:
      enabled: true
  endpoints:
    web:
      exposure:
        include: restart